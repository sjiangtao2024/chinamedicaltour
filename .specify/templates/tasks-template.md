---
description: "Task list template for feature implementation"
---

# Tasks: [FEATURE NAME]

**Input**: Design documents from `/specs/[###-feature-name]/`
**Prerequisites**: plan.md (required), spec.md (required for user stories), research.md, data-model.md, contracts/

**Tests**: Tests are REQUIRED when a change affects Smart CS critical behaviors (SSE, CORS, error mapping,
key rotation/cooldown, timeout/cancel). Prefer scriptable integration checks (`curl` / Node) and document how to run.

